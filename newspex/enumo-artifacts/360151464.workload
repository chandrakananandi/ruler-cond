(plug "bop" {& | ^ ->}
  (plug "uop" {~}
    (plug "var" {ba bb bc}
      (plug "lit" {true false}
        (filter (contains var)
          (filter (#lists < 2)
            (plug "lang" {lit var}
              {lit var (uop lang) (bop lang lang)})))))))
